{% extends 'base.html.twig' %}

{% block title %}Info de l’utilisateur{% endblock %}

{% block body %}
<section class="table-section">
    <h2>Info de l’utilisateur</h2>

    <div class="table-card">
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Roles</th>
                    <th>Date d'inscription</th>
                </tr>
            </thead>
            <tbody>
                {% if user %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.username }}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.roles|join(', ') }}</td>
                    <td>{{ user.registrationDate ? user.registrationDate|date('d/m/Y') : '-' }}</td>
                    <td>
                        <a href="{{ path('app_userdelete', {'username': user.username}) }}" class="btn"
                            onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?');">
                            Supprimer
                        </a>
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="6">L'utilisateur n'existe pas</td>
                </tr>
                {% endif %}
            </tbody>
        </table>
    </div>
</section>
{% endblock %}
